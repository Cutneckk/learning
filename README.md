Задача: 

    1. Напишите асинхронную функцию `countdown(name, seconds)`.

    - Она должна принимать два аргумента: `name` (имя таймера, строка) и `seconds` (количество секунд, целое число).
    - Функция должна печатать числа от `seconds` до 1, делая паузу в 1 секунду между выводами.
    - После вывода "1" функция должна напечатать сообщение `"{name}: Пуск!"`.

Реализация: Простая асинхронная функция с циклом от seconds до 1 в обратном порядке

    2.Представьте, что у вас есть два гонщика. Напишите асинхронную программу, которая имитирует их забег.
    
       1. Создайте корутину `racer(name, delay)`, которая "бежит" `delay` секунд (используя `asyncio.sleep`) и по завершении выводит сообщение `"{name} финишировал!"`.
       2. В главной функции `main` создайте двух гонщиков:
           - "Молния" с задержкой 2 секунды.
           - "Стрела" с задержкой 2.5 секунды.
       3. Запустите их "забег" одновременно с помощью `asyncio.gather`.
       4. Программа должна вывести, кто финишировал, по мере их прибытия.

Реализация: Асинхронная функция принимающая имя и время выполнения. main() вызывает сразу 2 функции параллельно и выводит результат выполнения спустя {delay} секунд

    3.Напишите асинхронную программу для проверки статуса нескольких веб-сайтов.
    
       1. Возьмите список URL-адресов: `['https://www.python.org', 'https://www.google.com', 'https://non-existent-domain-12345.org']`.
       2. Напишите корутину `check_status(session, url)`, которая делает GET-запрос по URL с помощью `aiohttp`.
       3. Корутина должна возвращать строку: `f"{url}: OK"` если код ответа 200, или `f"{url}: Ошибка {response.status}"` в противном случае.
       4. Не забудьте обернуть сетевой запрос в `try...except`, чтобы поймать ошибки соединения (например, для несуществующего домена) и вернуть сообщение вида `f"{url}: Не удалось подключиться"`.
       5. Используйте `asyncio.gather` для одновременной проверки всех сайтов и выведите результаты.

Реализация: Функция принимающая url и выводящая статус подключени. mian() вызывает поочередно первую функцию передавая в аргументы *распаковку list comprehension и соединение aiohttp

    4.Напишите программу, которая запускает долгую фоновую задачу и продолжает выполнять другую работу, не дожидаясь ее завершения.
    
       1. Создайте корутину `long_running_task()`, которая имитирует долгую операцию: печатает "Фоновая задача начала работу...", ждет 5 секунд, а затем печатает "Фоновая задача завершена.".
       2. В главной функции `main` запустите `long_running_task()` в фоне с помощью `asyncio.create_task()`.
       3. Сразу после запуска фоновой задачи `main` должна напечатать "Основная программа продолжает работу..." и затем
       выполнить цикл, который 3 раза с интервалом в 1 секунду печатает
       "Основная программа работает...".
       4. Убедитесь, что сообщения из основной программы и фоновой задачи выводятся параллельно.

Реализация: Функция создающая долгую фоновую задачу с помощью asyncio.sleep(). main() вызывает фоновую функцию продолжая основное выполнения. Тут я немного запутался, получал неправильный вывод. Решил строкой await asyncio.sleep(0) и await task в конце

    5.Смоделируйте процесс приготовления блюда, где одни действия зависят от других, а некоторые могут выполняться параллельно.
    
        1. Напишите три корутины-действия с имитацией времени через `asyncio.sleep`:
            - `prepare_ingredients()` — "Нарезка овощей..." (2 секунды).
            - `fry_meat()` — "Жарка мяса..." (4 секунды).
            - `boil_rice()` — "Варка риса..." (3 секунды).
        2. **Логика приготовления:** Нарезка овощей — это первый шаг, и без него нельзя начать ни жарить
        мясо, ни варить рис. Однако, после того как овощи нарезаны, жарка мяса и варка риса могут происходить **одновременно**.
        3. В `main` организуйте вызовы этих корутин в правильной последовательности. Сначала дождаться выполнения `prepare_ingredients()`, а затем одновременно запустить `fry_meat()` и `boil_rice()` с помощью `asyncio.gather`.
        4. Замерьте и выведите общее время приготовления.
